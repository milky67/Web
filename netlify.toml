[build]
  command = ""          # empty = no build needed
  publish = "."         # root folder contains index.html
  functions = "netlify/functions"

# Optional: clean redirects for functions
[[redirects]]
  from = "/.netlify/functions/*"
  to = "/.netlify/functions/:splat"
  status = 200
